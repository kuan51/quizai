# =============================================================================
# QuizAI Environment Variables
# Copy this file to .env.local and fill in the values
# =============================================================================

# --- Authentication (Required) ---
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
# Set to your deployment URL in production (e.g., https://quizai.example.com)
NEXTAUTH_URL=http://localhost:3000

# Comma-separated emails allowed to sign in
# Use "*" to allow all OAuth users (required in production)
# Requires server restart after changes
AUTHORIZED_EMAILS=

# --- OAuth Providers (at least one required) ---

# Google OAuth - https://console.developers.google.com/
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth - https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Discord OAuth - https://discord.com/developers/applications
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# --- AI Providers (at least one required) ---

# Anthropic - https://console.anthropic.com/
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# OpenAI - https://platform.openai.com/
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4-turbo-preview

# Default AI provider: "anthropic" | "openai" | "claude-code"
DEFAULT_AI_PROVIDER=anthropic

# --- Database (Required for Vercel deployment) ---
# For local development, you can use a local Postgres instance or Vercel Postgres
# Vercel will automatically set DATABASE_URL when you add Vercel Postgres storage
# Format: postgresql://user:password@host:port/database
DATABASE_URL=

# Required by @vercel/postgres SDK (used for runtime database queries)
# Set to the same value as DATABASE_URL for local development
# Vercel automatically sets this in production
POSTGRES_URL=

# --- Logging (Optional) ---
# Level: fatal | error | warn | info | debug | trace
LOG_LEVEL=info

# --- Environment ---
NODE_ENV=development
